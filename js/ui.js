function $(id){return document.getElementById(id)}function showModal(html){modal.innerHTML=`<div class="modalCard">${html}</div>`;modal.classList.remove("hide")}function closeModal(){modal.classList.add("hide");modal.innerHTML=""}function nav(id){document.querySelectorAll(".screen").forEach(s=>s.classList.add("hide"));$(id).classList.remove("hide");document.querySelectorAll(".tab").forEach(t=>t.classList.toggle("active",t.dataset.nav===id));if(id==="dashboard")renderDashboard();if(id==="workout")renderWorkout();if(id==="history")renderHistory();if(id==="calendar")renderCalendar();if(id==="exercises")renderExercises();if(id==="more")renderMore();if(id==="settings")renderSettings();window.scrollTo(0,0)}document.addEventListener("click",e=>{if(e.target.dataset.nav)nav(e.target.dataset.nav)})function switcher(){return `<label class="mini">Switch today's workout</label><select onchange="state.dayOverrides[todayKey()]=this.value;save();renderDashboard();renderWorkout()">${["Push A","Pull A","Run","Legs","Push B","Pull B","Rest"].map(x=>`<option ${x===todayWorkoutName()?"selected":""}>${x}</option>`).join("")}</select>`}