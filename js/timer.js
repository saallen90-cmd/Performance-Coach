let timer=null,timerRemaining=0;function startTimer(seconds,label="Rest"){clearInterval(timer);timerRemaining=seconds;renderTimer(label);timer=setInterval(()=>{timerRemaining--;renderTimer(label);if(timerRemaining<=0){clearInterval(timer);timer=null;try{navigator.vibrate&&navigator.vibrate([250,120,250])}catch(e){}}},1000)}function stopTimer(){clearInterval(timer);timer=null;timerDock.classList.add("hide")}function renderTimer(label){timerDock.classList.remove("hide");let m=Math.floor(timerRemaining/60),s=String(timerRemaining%60).padStart(2,"0");timerDock.innerHTML=`<div class="row"><div><div class="mini">${label}</div><div class="timerTime">${m}:${s}</div></div><div class="row"><button class="secondary small" onclick="startTimer(60,'Rest')">1:00</button><button class="secondary small" onclick="startTimer(90,'Rest')">1:30</button><button class="secondary small" onclick="startTimer(180,'Rest')">3:00</button><button class="ghost small" onclick="stopTimer()">Stop</button></div></div>`}